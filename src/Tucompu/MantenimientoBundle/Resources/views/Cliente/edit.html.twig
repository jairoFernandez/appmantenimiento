{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Editar Cliente</h1>

    {{ form(edit_form) }}
<hr>
        <a href="{{ path('cliente') }}" class="btn btn-info">
            Volver 
        </a>
   {{ form(delete_form) }}
{% endblock %}
